%p
  = t('hello', email: @resource.email)
%p= t('your_account_locked')
%p= t('click_link')
%p= link_to t('unlock_my_account'), unlock_url(@resource, unlock_token: @token)
%br
%p ※このメールに対する返信は受け付けておりません。
%br
%p= EasySettings.service_name